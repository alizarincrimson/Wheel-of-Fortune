class Player {
  constructor(name, id) {
    this.name = name;
    this.id = id;
    this.totalScore = 0;
    this.roundScore = 0;
  }
  
  displayScore() {
    // we want to access the players name and their score. 
  }
}

export default Player;