class Player {
  constructor(name) {
    this.name = name;
    this.totalScore = 0;
    this.roundScore = 0;
  }
  displayScore() {
    // we want to access the players name and their score. 
  }
}

export default Player;