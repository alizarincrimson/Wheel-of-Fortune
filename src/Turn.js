class Turn {
  constructor(guess) {
    this.guess = guess;
  }
  buyVowel(player) {
    // player.score = player.score - 100
    // prompt to select available vowel from vowels array
    // chosen vowel spliced from array
  }

  spinWheel(player) {
    // wheel.getWheelValue()
    // if (wheel.getWheelValue().spin === 'Bankrupt') {
    // player.score === 0
    // return `Your score has been reset to 0`
    // } else if (wheel.getWheelValue().spin === 'Lose A Turn'){
    // switch to next player
    // return `You have lost a turn.`
    // } else {
    // player selects consonant from consonants array
    // consonant is spliced from consonants array
    // solvePuzzle(guess)
    // }
    //
    //
  }

  solvePuzzle(player, guess) {
    // if (guess === puzzle.word.letter) {
    //  player.score = player.score + wheel.getWheelValue().spin  
    //  return `Correct! ${player} receives ${wheel.getWheelValue().spin}`
    // } else if (guess != puzzle.word.letter) {
    //  player.score = player.score - wheel.getWheelValue().spin
    //  return `You've guessed incorrectly. Your score is now ${player.score}.`
    // } else if (guess === puzzle.word) {
    //  player.score = player.score + wheel.getWheelValue().spin 
    //  return `Congratulations! You've solved the puzzle!`
    // }
    //}
  }
}