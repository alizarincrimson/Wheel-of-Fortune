class Wheel {
  constructor(dataFilePath) {
    this.dataFilePath = dataFilePath;
  }
  getWheelValue() {
    //use Math.random to select Wheel value
    // let wheel = [200, 300, 500, 600, 900, 'Bankrupt', 'Lose A Turn']
    // let spin = function getRandomInt(wheel) {
    // return Math.floor(Math.random() * Math.floor(wheel));
}
  }
}